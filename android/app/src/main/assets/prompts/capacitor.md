# Capacitor Project Prompt

When working on this Capacitor project:
- Treat the web app and native shells as separate integration surfaces.
- Inspect `capacitor.config.*`, `ios/`, and `android/` before changing native behavior.
- Avoid modifying generated native files unless the task requires it.
- Check plugin compatibility and platform-specific permissions.
